/**
 * Stable copy + version for lawful consent logs (audit).
 * Bump version if you change the checkbox wording or broaden use of data.
 */
export const EMAIL_CAPTURE_CONSENT_VERSION = 'jf-email-report-v1-2026-05'

/** Exact checkbox wording — keep in sync with UI. */
export const EMAIL_CAPTURE_CONSENT_LABEL =
  'I agree to receive my report and occasional JobFit AI updates.'
